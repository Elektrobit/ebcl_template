# Use elbe as image builder
type: elbe
# CPU architecture of the target
arch: 'arm64'
# Relative path to the kiwi appliancd
image: systemd-grub-aarch64.xml
# Result file name pattern
result: sdcard.img
