base: base.yaml
# Reset kernel package
kernel: null
# Name of the archive.
name: ebcl
# Packages to install in the root tarball
packages:
  # SSH server
  - openssh-server
  # Other basic tools
  - openssh-client
  - locales
  - bash
  - util-linux
  - cron
  - file
  - findutils
  - iproute2
  - iptables
  - iputils-ping
  # Apt package manager
  - apt
  - gnupg
  # Editors
  - vim
  - nano
  # required for example my-json-app
  - libjsoncpp25
  # gdbserver for debugging
  - gdbserver
  # psmisc for 'killall' cmd
  - psmisc
  # rsync for app deployment during development
  - rsync
# Add packages for app development example
sysroot_packages:
  # Add libjson package
  - libjsoncpp-dev
  # Add the qt packages
  - qt6-base-dev
  - qt6-base-dev-tools
  - libgl1-mesa-dev
