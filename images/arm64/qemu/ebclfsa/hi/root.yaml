base: ../base.yaml
# reset the kernel value
kernel: null
# Name of the archive.
name: root
# Packages to install in the root tarball (right now none)
# packages:
host_files:
#  - source: root/*
  - source: $$RESULTS$$/app/install/bin/*
    destination: bin/
  # Install li components as well (The path is strange, I think there is a bug in the path parser)
  - source: $$RESULTS$$/../../build/li/app/install/*
