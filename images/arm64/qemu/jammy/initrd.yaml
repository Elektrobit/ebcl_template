# Derive values from base.yaml - relative path
base: base.yaml
# Root device to mount
root_device: /dev/vda1
# List of kernel modules
modules:
  - virtio_blk # virtio_blk is needed for QEMU
  - autofs4 # Wanted by systemd
