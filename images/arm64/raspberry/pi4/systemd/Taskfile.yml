version: '3'

vars:
  arch: 'aarch64'
  partition_layout: ../image.yaml
  root_filesystem_spec: root.yaml

includes:
  rpi:
    taskfile: ../../raspberry.yml
    flatten: true

tasks:
  default:
    cmds:
      - task: rpi:build_image
